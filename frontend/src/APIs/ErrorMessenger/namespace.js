const common = {
	// for app.js
	getDataset: "common.getDataset",
};
const login = {
	auth: "login.auth",
};
const landing = {};

const container = {
	addUser: "container.addUser",
	getUsers: "container.getUsers",
	createContainer: "container.create",
};

const registration = {};

export default { common, login, landing, container, registration };
